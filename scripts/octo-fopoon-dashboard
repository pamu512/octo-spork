#!/usr/bin/env bash
# Textual Fopoon dashboard: CPU/GPU, Docker, tracer thought log, graceful agent stop.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="${ROOT}/src${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m ui.dashboard "$@"
